# CIKUR GO Internal AI Upgrade V4 — Clean Package

Main project files keep their ORIGINAL filenames:

- bcgo.js
- bcgo.html
- cikur-config.js

Internal AI supporting modules are isolated under `internal-ai/`.

Original/baseline files are kept under `BACKUP_ORIGINAL/`.

This package is an integration candidate/test build. It does not intentionally replace
Medicine or Executor and does not add third-party AI/API dependencies.
