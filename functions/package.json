{
  "name": "bcgo-internal-executor",
  "version": "1.0.0",
  "private": true,
  "description": "BCGO internal deterministic patch executor without AI or third-party API",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "lint": "echo \"No lint configured\"",
    "test": "node --check index.js && node --check patchExecutor.js && node --check patchValidator.js && node --check config.js"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.0"
  }
}
