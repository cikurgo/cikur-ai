CIKUR GO / BCGO — INTERNAL BRAIN + BCGO INTEGRATION CANDIDATE — 3.4.0-INSTRUCTION-CONSTITUTION
Generated: 2026-09-07

Deployment overlay files:
- bcgo.js (BCGO MASTER NERVE SYSTEM v2.16.4 + LIVE CHAT COMMAND SYNC + CONTEXT STATE)
- bcgo.html (BCGO monitor UI v2.16.4-chatlive5)
- cgo-runtime-adapter.js (master gateway)
- cgo-ai-core.js
- cgo-ai-guardian.js
- cgo-ai-cognition.js
- cgo-ai-knowledge.js
- cgo-ai-investigator.js
- cgo-ai-investigation-engine.js
- cgo-ai-memory.js
- cgo-ai-logic.js
- cgo-ai-runtime-adapter.js
- cgo-ai-browser-adapter.js
- cgo-instruction.js (CGO Behavioral & Intelligence Constitution)

Verified contract:
BCGO_STATE -> cgo-runtime-adapter.install() -> Browser Bridge -> active internal brain.
BCGO source scanner provides sourceScan/fileNerves/relations.
Chat delegates to the internal Browser Bridge for explicit Perbaiki/Fix/Repair/Patch commands.
Execution is delegated only to an injected internal execution target; the brain does not write source directly.

Safety/integrity:
- No external AI provider/API in the internal brain assembly.
- No Cloud Functions dependency in the internal brain assembly.
- bcgo.html is excluded from BCGO organ telemetry registry.
- bcgo.js remains monitor/diagnostic infrastructure, not an organ telemetry target.
- Medicine/Executor files are intentionally not included or modified.
- Firebase SDK remains BCGO's data/realtime layer; it is not an AI brain dependency.


CHAT NATURALITY PATCH — 2026-09-06
- Browser chat now recognizes multiple explicitly mentioned files in one request.
- Cross-file questions are treated as generic comparison investigations; no file pair is privileged or hard-coded.
- Follow-up wording such as “lakukan perbaikan pada code bagian itu” continues the last chat case without requiring the user to repeat the filename.
- Repair response remains proof-bound: no source mutation occurs until root cause + exact source proof + internal Guardian AUTO_ALLOWED + execution target are satisfied.
- No second manual approval is introduced.
- Medicine and Patch Executor files are not modified by this patch.


SYNC + CHAT HARDENING PATCH — 2026-09-06
- BCGO_STATE is now read live by CGO chat on every message; an older bridge snapshot can no longer override the current BCGO cycle/stage.
- The browser bridge exposes getBCGOState() and publishes BCGO cycle/stage metadata with its internal-AI snapshot.
- BCGO HTML now verifies Integrity against the live CGO bridge cycle instead of comparing BCGO state with its own UI copy.
- Cross-file chat preserves the user's file order and creates a dedicated CGO chat case, preventing chat requests from hijacking a telemetry case.
- Cross-file chat context carries scanner findings into the investigation evidence chain and related files are read from the live deployment source before generic probing.
- Internal probe evidence no longer causes the same investigation generation to restart; only new authoritative BCGO evidence invalidates that generation. This removes duplicate SOURCE_READ / EVIDENCE_ID_COLLISION behavior.
- Follow-up repair commands continue the last chat context and remain proof-bound. A repair intent is retained while investigation/solution preparation is incomplete; it is not silently discarded. No source mutation occurs without verified root cause, exact source, concrete solution, Guardian AUTO_ALLOWED, and an injected internal execution target.
- The previous repair continuation bug that passed the resolved source filename as an investigation case ID is removed; continuation is now bound to the dedicated CGO chat case ID.
- CGO exposes an internal acceptRepairProposal bridge for a verified Medicine proposal; it re-reads the live source, checks the current source fingerprint and exact original snippet, then resumes the pending chat repair intent.
- The deterministic Executor receives the target filename on read/write calls so a bound executor can remain file-specific without relying on hidden mutable selection state.
- Chat waits for the internal runtime instead of silently falling back to a stale presentation-only answer. Technical sync metadata is kept in the monitor state rather than appended to every natural-language reply.

VALIDATION
- All deployment JavaScript files: node --check PASS.
- All inline JavaScript blocks in bcgo.html: syntax PASS.
- Live-state smoke: CGO chat reported the same BCGO cycle after window.BCGO_STATE was updated.
- Cross-file smoke: comparison requests resolve whichever files are named in live BCGO_STATE/sourceScan.
- Repair smoke: incomplete proof was correctly refused and the command intent remained pending.
- Actual source investigation smoke: the named cross-file case read bcgo-admin.html and agentcgo.html, traced agentRegistrationSubmit, and bound the verified exact source to agentcgo.html without source mutation.
- Proposal/execution harness: verified solution binding reaches the internal executor path; the harness deliberately rejected readback when its fake target did not persist the write, confirming the persistence gate remains active.
- No external AI provider/API or Cloud Functions dependency was added.


GENERIC ORGAN REGISTRY
- Reserved standby organs: payment.html, merchant.html, new-service.html, new-engine.js.
- Reserved entries are optional until their real deployment source exists; absence is NOT a scanner failure.
- CGO chat resolves requested files dynamically from BCGO_STATE.systemOrgans/sourceScan, not from a privileged pair.
- Cross-file comparison, dependency tracing, source reading, and repair targeting remain generic.


CHAT LIVE HARDENING — 2026-09-06 (2.2.0)
- Chat now keeps an explicit conversational session: last intent, target files, comparison context, active case, and pending repair command.
- Follow-up phrases such as “yang tadi”, “bagian itu”, “lanjut”, “hasilnya”, and “lakukan perbaikan” resolve against the same chat case when context is unambiguous.
- Exact source is preferred over the original target when investigation proves that the root cause lives in another file.
- Simple conversational messages such as “BCGO?”, “oke”, “iya”, and “lanjut” no longer fall through to a generic technical answer.
- Investigation progress is surfaced in the same chat as short natural messages; these messages describe activity only and never substitute for proof.
- The visible chat no longer appends raw “[SYNC] cycle …” diagnostics to every answer. Live cycle/state remains available in the monitor telemetry.
- Repair intent survives incomplete proof and continues from the same case; execution remains blocked until the existing proof, Guardian, integrity, and executor gates pass.


CHAT LIVE STATUS/CONTEXT DEEP AUDIT (2026-09-06):
- Exactly one conversational brain: BCGOBrain delegates to CIKURInternalAIRuntime.
- Chat context preserves the active case, comparison files, proven exact source and pending repair intent.
- Follow-up phrases such as "yang tadi", "bagian itu", "hasilnya" and "lanjutkan" do not spawn a fresh case accidentally.
- Repair commands never restart a completed root-cause/source investigation merely because a concrete solution is still pending.
- Proven source remains bound to the same case when investigation corrects the initial target to another file.
- Live investigation events are rendered as human-readable progress updates.
- No external AI/API and no direct source mutation by the chat brain.


CHAT LIVE FIX 2.4:
- Investigation progress is rendered as one replaceable live-status bubble, never as repeated chat history messages.
- Semantic duplicate progress is suppressed.
- User/BCGO conversation remains separate from operational progress.
- No Medicine/Patch Executor/source mutation changes are introduced by this chat-only fix.


CHAT LIVE ROOT FIX — 2026-09-06 (2.6.0):
- All internal module imports now share the same build token 20260907-0900-constitution-connectivity1.
- The master gateway no longer points at the previous cached Browser Bridge build.
- Browser chat is a contextual conversation layer: corrections/feedback stay in the same dialogue, active-case questions use live case state, and technical instructions remain bound to case/evidence/proof.
- Operational investigation progress remains a replaceable LIVE status and is never promoted into chat history.
- No external AI/API and no direct source mutation are introduced.


CHAT LIVE ORCHESTRATOR 2.7.0 — PRE-DEPLOYMENT HARDENING
- Human work requests are parsed as a work plan (CHECK → INVESTIGATE → PROVE → optional REPAIR), not as a single keyword intent.
- An omitted/ambiguous target never falls back to BCGO lastTelemetryFile; the chat asks for clarification instead of guessing.
- A file name supplied as the answer to clarification resumes the pending work plan and creates/continues the same chat case.
- Follow-up references (file itu, bagian itu, yang tadi, lanjut) resolve only against established conversation context.
- System-status questions are informational and do not create a source-investigation case merely because they contain the word cek.
- Chat response, live operational progress, and investigation evidence are separate channels.
- BCGO loader cache tokens were aligned to the same 20260907-0900-constitution-connectivity1 build as the master gateway and Browser Bridge.


CHAT LIVE RE-AUDIT 3.0.0
- Root defect fixed: correction/feedback turns that explicitly name a file are resolved as target corrections before the generic correction handler.
- Example fixed: after a prior target index.html, “Iya maksud saya file bcgo-engine.js, coba di cek” now creates/continues the CGO chat case for bcgo-engine.js instead of replying from index.html.
- Explicit human file names remain higher priority than telemetry, remembered target, or previous case.
- Unknown explicit filenames are never silently replaced; basename-nearest candidates are offered only as clarification.
- Telemetry lastTelemetryFile remains observation context only and cannot hijack a human work target.
- Follow-up result/status/repair messages remain attached to the corrected CGO chat case.
- No external AI/API and no direct source mutation were introduced.


CHAT ORCHESTRATOR 3.0.1
- Human explicit target outranks telemetry and stale case context.
- Pure continuation never creates a new CHAT_REQUEST and never invalidates proof.
- Pending REPAIR intent survives continuation.
- Conversation case is authoritative; lastChatCaseId cannot resurrect abandoned work.
- Filename parsing is extension-agnostic; BCGO_STATE/sourceScan decides whether a target exists.
- Every chat turn updates conversational state before early returns.
- No external AI/API; no direct source mutation by the browser bridge.
- Background investigation from an older case cannot overwrite the current conversational focus; async proof updates are accepted only when that case is still the active chat case.
- A target correction detaches the old case and clears its pending repair intent; the corrected target receives a fresh work case when the human resumes work.
- A new work request always receives a unique conversation case, preventing old evidence/proof for the same filename from being silently reused.
- “Perbaiki bagian itu” and similar repair follow-ups are routed to the existing case before generic CHECK/INVESTIGATE routing, so repair intent does not get trapped behind a fresh chat request.
- Cross-file chat acknowledges the complete comparison set without treating the first file as canonical.

COMMAND-TO-COMPLETION HARDENING — 2026-09-07
- Chat repair intent now has a complete runtime lifecycle: proof → Guardian → Executor → Runtime validation → RESOLVED/REOPENED.
- A successful internal Executor result with verified readback/execution is immediately consumed by the Runtime validation gate; the brain never marks a case resolved from a chat sentence alone.
- Execution failures remain non-resolved and preserve the integrity/error path; no silent success is reported.
- The conversational case remains bound while asynchronous execution/validation completes, and the chat session is cleared from pending REPAIR only after the case reaches RESOLVED.
- No Medicine, Patch Executor, Firestore Rules, or deployment source outside this candidate was modified.

COMMAND-TO-COMPLETION E2E
- In-memory target: exact source fingerprint verified.
- Root cause: independently supported and verified.
- Exact source + concrete proposedCode: verified and bound.
- Guardian: AUTO_ALLOWED under CIKUR-INTERNAL-AUTO-1 / INTERNAL_AUTO.
- Executor: PATCH_APPLIED_AND_EXECUTED with readBackVerified=true and executed=true.
- Runtime validation: FIXED_VERIFIED.
- Final case state: RESOLVED.


ADVERSARIAL COMMAND-TO-COMPLETION DEEP AUDIT — 2026-09-07
- Authorization expiry is fail-closed: an existing expired execution intent is no longer silently re-authorized by execute().
- Authorization in-flight is fail-closed: concurrent execution cannot mint a second authorization for the same pending intent.
- Replay remains blocked by both Runtime authorization ledger and Executor consumed ledger.
- Source fingerprint is re-checked immediately before mutation; a source change during the execution window is rejected before write.
- Deterministic Executor supports optional atomic writeIfUnchanged(expected,next,file); an atomic rejection aborts without mutation.
- Rollback preserves the explicit target filename on write/read-back.
- Validation is no longer a boolean trust gate: it requires execution provenance (authorizationId/planId/file/status/fingerprints/readback/executed) and an independent current-source fingerprint verification through the Executor.
- A fabricated success/FIXED_VERIFIED validation cannot resolve a VALIDATING case when the current source does not match the executor's verified afterFingerprint.
- Validation is asynchronous and is awaited by Command-to-Completion before reporting RESOLVED.

ADVERSARIAL TEST RESULTS
- Success path: PASS
- Authorization expiry: PASS
- Authorization replay: PASS
- Stale source before execution: PASS
- Source changed during execution window: PASS
- Failed execution + rollback: PASS
- Fabricated validation / false positive: PASS
- Atomic write rejection: PASS
- Concurrent duplicate execution: PASS
- All candidate JavaScript syntax checks: PASS
- Aggregate: 9/9 adversarial scenarios PASS.

Residual boundary intentionally preserved:
- Without an atomic writeIfUnchanged contract supplied by the real Patch Executor, a final microscopic TOCTOU window can theoretically exist between the last pre-write read and a non-atomic write. The candidate therefore uses the strongest available pre-write fingerprint guard and supports the stronger atomic contract without modifying the separate Patch Executor in this stage.


FINAL INTEGRATION / ADVERSARIAL HARDENING — 2026-09-07
- Execution rollback now uses writeIfUnchanged(next,current,file) when available, so a rollback cannot overwrite a newer external source change after a failed execution.
- Non-atomic rollback now re-reads the current source and refuses rollback if the source changed after the patch, rather than clobbering a concurrent change.
- Command-to-completion validation remains asynchronous and proof/provenance bound: authorizationId, planId, file, status, before/after fingerprints, read-back, execution, and current-source verification must all agree before RESOLVED.
- Final integration smoke: master gateway -> Browser Bridge -> live BCGO state -> chat target resolution passes.
- Adversarial runtime checks cover expired authorization, authorization replay, source drift before execution, failed execution rollback, validation provenance mismatch, and successful completion.
- No deployment/GitHub source was modified.


CONVERSATION CONTINUITY STRESS HARDENING — 2026-09-07
- Browser Bridge now persists only bounded conversational transcript/session/context metadata in versioned localStorage.
- Runtime case IDs, proof, evidence, source fingerprints, authorization, execution and validation state are never trusted after refresh.
- On refresh, CGO restores the dialogue context and marks the old runtime case as stale; work resumes only by creating a fresh case and re-verifying live source/evidence.
- Bare contextual status questions such as “gimana?” after refresh report the restored target without fabricating progress or resurrecting stale proof.
- “lanjut” after refresh preserves a pending REPAIR intent as intent only, then creates a fresh investigation case.
- Follow-up “cek yang itu” on the active target reuses the same case instead of creating duplicate CHAT_REQUEST evidence/cases.
- Target correction detaches the old case; explicit correction remains authoritative over prior conversation/telemetry context.
- Stress sequence exercised: check → status → follow-up → continue → acknowledgement → correction → target switch → re-check → result → continue → repair → wait → status → refresh → status → continue → follow-up check → repair.
- Observed result: conversation target continuity preserved, refresh recovery preserved, stale proof rejected, pending repair intent preserved, and follow-up check did not mint a duplicate case after the active target was restored.
- No external AI/API, Medicine, Patch Executor, Firestore Rules, or direct source mutation policy was added or changed by this hardening pass.

GENERIC FILE TARGET + FAILURE INJECTION STRESS — 2026-09-07
- Target resolution remains generic and extension-agnostic; the chat bridge does not whitelist specific filenames.
- Stress harness exercised arbitrary targets: alpha.ts, beta.py, config.yaml, weird-file.sql, and gamma.php.
- Unknown target does not fall back to telemetry or another filename; it enters clarification/fail-closed handling.
- Multi-file request preserves both explicitly named files for comparison rather than treating the first as canonical.
- Target correction from an unknown target to a valid target succeeds and clears the stale clarification state.
- Source availability remains authoritative through BCGO sourceScan/systemOrgans; filename syntax alone never establishes that a file exists.
- No source mutation, external AI/API, Medicine, Patch Executor, or Firestore Rules changes were made during this stress phase.
- Existing adversarial runtime protections remain in force: expired authorization, replay, source drift, rollback, fabricated validation, atomic rejection, and concurrent duplicate execution are fail-closed as previously recorded.
- Result: generic target resolution PASS; unknown-target fail-closed PASS; multi-file comparison target recognition PASS; target correction PASS.


CONVERSATIONAL INTELLIGENCE UPGRADE 3.3.0 — 2026-09-07
- CGO is now explicitly modeled as the conversational/intelligence layer speaking through the BCGO UI; BCGO remains the live operational/telemetry environment.
- Browser Bridge adds a bounded dialogue classifier and conversational response layer for identity, CGO/BCGO role, capability, greetings, thanks, apologies, casual warmth, farewell, and emotion-boundary questions.
- Pure conversational turns do not create work cases, do not manufacture evidence, and do not hijack an active technical case.
- Technical work remains higher priority than casual language; conversation can transition into evidence-first engineering without losing the established target/case.
- Conversation session now records safe dialogue metadata (mode/topic/tone) alongside the existing bounded transcript. Runtime proof, authorization, fingerprints, execution, and validation remain non-persistent and untrusted after refresh.
- Cognition exposes dialogueMode() to distinguish conversational, informational, and work-oriented speech while preserving the existing evidence/proof gate.
- BCGO UI wording now makes the architecture explicit: CGO communicates through BCGO rather than presenting the sensor/monitor itself as the conversational intelligence.
- No external AI/API, direct source mutation, Medicine, Patch Executor, Firestore Rules, or deployment source was added or modified.

CONVERSATIONAL INTELLIGENCE VERIFICATION — 2026-09-07
- Build/cache token for the full baseline is synchronized to 20260907-0900-constitution-connectivity1.
- Pure conversational checks: identity, CGO/BCGO role, capability, greeting, thanks, apology, casual warmth, and emotion-boundary response all route through the CGO conversational layer.
- Contextual conversational follow-up such as “gimana?” / “terus” can continue a prior conversational topic when no technical case is active.
- System inventory question (“ada apa saja di sistem?”) reads the live BCGO organ registry and reports only observed entries.
- Technical continuity checks remain intact: explicit target, status/progress, continuation, repair intent, unknown-target fail-closed, and multi-file comparison remain evidence/case bound.
- Refresh restoration preserves bounded dialogue transcript and safe dialogue metadata; runtime proof/case remains stale and is not resurrected as trusted.
- JavaScript syntax and inline HTML JavaScript checks PASS.
- Functional conversation harness PASS; technical continuity regression harness PASS.


CGO INSTRUCTION CONSTITUTION — 2026-09-07
- New baseline module: cgo-instruction.js. It is the central CGO Behavioral & Intelligence Constitution.
- The module centralizes identity, conversation behavior, intent priority, reasoning boundaries, investigation principles, repair boundaries, execution-gate requirements, and personality constraints.
- Browser Bridge consumes the constitution for conversational classification and intent precedence; Cognition consumes it for dialogue-mode classification; Logic and Guardian bind their metadata/namespace to the same constitution; the master runtime exposes the constitution as part of the single gateway.
- The constitution is behavioral guidance and does not replace deterministic proof, Guardian, investigation, runtime authorization, source fingerprint, or validation gates.
- Conversation-first classification prevents ordinary questions such as “Apa yang sedang kamu kerjakan?” from being mistaken for a technical “kerjakan” command when no explicit technical target/action is present.
- Contextual short follow-ups such as “kenapa?” may remain attached to a conversational topic without being misclassified as a root-cause request.
- No external AI/API, Medicine, Patch Executor, Firestore Rules, or deployment source was added or modified by this instruction-layer milestone.
- Full baseline increases from 14 files to 15 files.
- Build/cache token: 20260907-0900-constitution-connectivity1
- Status: INSTRUCTION LAYER INTEGRATED — NOT FINAL.


ACTIVE INVESTIGATION LIFECYCLE FIX — 2026-09-07
- Fixed the conversational investigation lifecycle that could appear stuck after the engine reached SOURCE_VERIFIED.
- SOURCE_VERIFIED is now treated as a valid diagnostic completion boundary when root cause and exact source are both proven, rather than as an indefinite WORKING state.
- Browser Bridge now emits ACTIVE_INVESTIGATION_COMPLETED with an explicit conclusion, phase, blockers, evidence/probe counts, and nextAction.
- Chat status now reports the actual terminal diagnostic result and the concrete next stage instead of repeatedly saying it is still investigating.
- BCGO chat UI consumes the completion event and replaces the transient LIVE indicator with a clear completion/blocked state.
- No automatic source mutation was added; a missing concrete solution remains explicitly blocked rather than fabricated.
- No external AI/API, Medicine, Patch Executor, Firestore Rules, or deployment source was modified.
- JavaScript syntax validation and the 15-file baseline packaging must be re-run after this milestone.
- Status: LIFECYCLE FIX — AWAITING BROWSER VALIDATION.

============================================================
CGO CONSTITUTION UPGRADE — 2026-09-07
============================================================
Version: 1.2.0-CGO-CONSTITUTION
Constitution: CGO-CONSTITUTION-1.2

Source basis:
- CGO_003_COMMUNICATION_ENGINE.md Version 1.0 (official CIKUR GO communication standard)

Implemented in cgo-instruction.js:
- Expanded central Constitution to the agreed nine-domain structure:
  IDENTITY, CONVERSATION, HUMAN BEHAVIOR, COMMUNICATION STYLE,
  INTELLIGENCE, SYSTEM, REPAIR, RESPONSE, BOUNDARIES.
- Added listen-first and intent-first communication rules.
- Added explicit conversation continuity/reference/topic-transition rules.
- Added human-behavior guidance for positive, sad, confused, disappointed,
  excited, joking, casual, angry, praised, and criticized contexts.
- Added formal/casual/short/complex/variation/emoji communication guidance.
- Added technical response contracts and completion contracts.
- Added golden-rule response evaluation: true, polite, understandable,
  helpful, and aligned with CIKUR GO.
- Added createConversationState() as a safe conversational-state model.
- Expanded dialogue classification for contextual follow-ups and human-behavior signals.

Safety / architecture preserved:
- No external AI/API added.
- No automatic source mutation added.
- No Guardian, authorization, execution, rollback, or validation gate bypass.
- Historical memory remains advisory only and is never proof.
- Medicine and Patch Executor files were not modified.
- Firestore Rules were not modified.

Validation status:
- cgo-instruction.js syntax: PASS
- ESM import/export smoke test: PASS
- Constitution section inventory: PASS
- Conversation regression classification smoke test: PASS
- Browser validation: NOT YET PERFORMED
============================================================


CGO CONSTITUTION + CONNECTIVITY INVESTIGATION UPGRADE — 2026-09-07
- Constitution advanced to CGO-CONSTITUTION-1.4.
- Added explicit system connectivity law: source topology must be observed; declared references are distinguished from runtime loading; missing dependencies and unproven orphan candidates are surfaced without automatically declaring root cause.
- Active Investigation Engine advanced to 2.3.0-CONNECTIVITY-AWARE-INVESTIGATION.
- Added source-level connectivity probe across the available BCGO source surface: JS imports/exports/dynamic references and HTML script src references are compared against the live source registry.
- Missing referenced files are emitted as SOURCE_DEPENDENCY_MISSING evidence; available files without inbound references are emitted only as SOURCE_ORPHAN_CANDIDATE (UNVERIFIED).
- Connectivity is now an investigation dimension before causal conclusion, but file existence or an orphan candidate alone is never treated as root-cause proof.
- All module cache/build tokens were aligned to 20260907-0900-constitution-connectivity1 so the browser cannot silently retain the previous engine build.
- No external AI/API added. No direct source mutation added. Medicine, Patch Executor, Firestore Rules, and unrelated deployment source were not modified.
- Browser validation remains REQUIRED before declaring deployment PASS.
