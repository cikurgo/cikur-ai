CGO CHAT BRAIN PRIMARY + RADAR UPGRADE
2026-09-17

Changed:
- customer/cgo-customer.js: Brain is attempted on every non-transactional conversational turn; keyword gate removed. Brain only overrides when it can return evidence-bound handled=true.
- brain/cgo-ai-browser-adapter.js: added reasonChat(), capabilities(), radar/presence resolver contract, findNearbyAgents(), getPresenceContract().
- cgo-app-bootstrap.js: connects live internal BCGO presence resolver into the Brain radar capability.

Safety:
- No external AI/API added.
- No Firebase auth/login path changed.
- No Medicine/Executor/Captain changes.
- Radar remains UNKNOWN when live BCGO presence is unavailable/stale/unverified.
- Existing Customer Conversation generator remains fallback for ordinary natural conversation and specialized service intents.

Validation:
- node --check passed for all three changed JS files.
- Browser adapter import/install smoke test passed.
